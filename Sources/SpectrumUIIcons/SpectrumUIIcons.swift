/// Root namespace for the Icons layer of SpectrumUI.
///
/// Icons exposes Spectrum's icon assets and SF Symbol bridging. It depends on
/// `SpectrumUIFoundations` for tokens (sizing, color) and is consumed by Atoms and
/// Molecules wherever an icon is required.
public enum SpectrumUIIcons {
    /// Stable string identifier for this product. Useful in diagnostics and tests.
    public static let moduleName = "SpectrumUIIcons"
}
