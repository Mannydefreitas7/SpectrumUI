/// Root namespace for the Atoms layer of SpectrumUI.
///
/// Atoms exposes primitive components (Button, Checkbox, TextField, Badge, and the like).
/// It depends on `SpectrumUIFoundations` for tokens and theming, and is consumed by
/// Molecules to build composed components.
public enum SpectrumUIAtoms {
    /// Stable string identifier for this product. Useful in diagnostics and tests.
    public static let moduleName = "SpectrumUIAtoms"
}
